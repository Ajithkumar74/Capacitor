import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.virtusa.amz',
  appName: 'amz-app-ui',
  webDir: 'dist/amz-app-ui/browser'
};

export default config;
